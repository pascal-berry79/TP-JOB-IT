{% extends 'base.html.twig' %}

{% block title %}Job It - Accueil{% endblock %}

{% block body %}
    {% for job in jobs %}
        <a href=""></a>
        <hr>
        <div>
            <img src="images/logos/{{job.logo}}" alt="{{job.entreprise}}">
            <div>
                <p>{{job.entreprise}}</p>
                <p></p>
                <ul>
                    <li>{{job.contrat}}</li>
                    <li>{{job.lieu}}</li>
                    <li>{{job.pays}}</li>
                </ul>
            </div>
        </div>
    {% endfor %}
{% endblock %}
